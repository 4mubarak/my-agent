{
  "name": "my-agent",
  "version": "1.0.0",
  "description": "AI-powered code review agent using Google's Gemini model",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "build": "bun build index.ts --outdir ./dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-sdk/google": "^2.0.14",
    "ai": "^5.0.44",
    "simple-git": "^3.28.0",
    "zod": "^4.1.8"
  }
}
